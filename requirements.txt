# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
python-multipart>=0.0.6

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0

# ── Computer Vision & Imaging ─────────────────────────────────────────────────
opencv-python-headless>=4.8.0,<5.0.0
numpy>=1.24.0
Pillow>=9.0.0

# ── AI Face Recognition — Tier 1: InsightFace (Python 3.14 compatible) ───────
# Uses ONNX Runtime — no TensorFlow needed
insightface>=0.7.3
onnxruntime>=1.16.0          # CPU inference (use onnxruntime-gpu for NVIDIA GPU)

# ── AI Face Recognition — Tier 2: DeepFace (requires Python ≤ 3.12) ─────────
# deepface requires TensorFlow which only supports Python 3.9-3.12
deepface>=0.0.93
tf-keras>=2.16.0

# ── Deep Learning (PyTorch — already present in original venv) ───────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.28.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
watchfiles>=0.20.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0.0
httpx>=0.24.0
anyio>=3.6.0
